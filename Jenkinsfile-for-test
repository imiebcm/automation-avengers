pipeline {
  agent {
    docker {
      image 'python'
    }
  }
  stages {
    stage ('Build') {
      steps {
        sh 'python -m py_compile src/app.py'
      }
    }
    
    stage ('Test') {
      steps {
        sh 'py.test --junit-xml test-reports/results.xml tests/test_apps.py'
      }
      post {
        always {
          xunit 'test-reports/results.xml'
        }
      }
    }
  
  }
}